{
  "name": "nexter",
  "version": "1.0.0",
  "description": "learning css grid with nexter project",
  "main": "index.js",
  "scripts": {
    "devserver": "live-server --browser=firefox",
    "test": "echo \"Error: no test specified\" && exit 1",
    "compile:sass": "node-sass sass/main.scss css/style.css -w"
  },
  "author": "severin denys",
  "license": "ISC",
  "devDependencies": {
    "node-sass": "^7.0.1"
  }
}
